@model RegionModel
<footer id="page-footer" class="page-row">
    @if (WebRequestContext.Current.IsSessionPreview)
    {
       @await Html.PartialAsync("Partials/XpmButton")
    }
    <div class="container-fluid page-border">
        @Html.DxaRegions(exclude: "Links", containerSize: 12)
        <hr />
        @Html.DxaRegion("Links")
    </div>
</footer>