query category($namespaceId: Int!, $publicationId: Int!, $categoryName: String) {
    category(namespaceId: $namespaceId, publicationId: $publicationId, categoryName: $categoryName) {
        ...KeywordFieldsWithCustomMetadaFields
    }
}
